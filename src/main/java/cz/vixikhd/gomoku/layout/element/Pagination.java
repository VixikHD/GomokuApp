package cz.vixikhd.gomoku.layout.element;

import javafx.scene.Node;

public class Pagination extends javafx.scene.control.Pagination {
	public Pagination(int pageCount, int currentPage) {
		super(pageCount, currentPage);
	}
}
